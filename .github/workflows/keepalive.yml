name: Keep Render API Alive

on: 
  schedule:
    - cron: "*/14 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render API
        run: |
          curl -f -s https://flight-api-9clq.onrender.com/ || exit 1